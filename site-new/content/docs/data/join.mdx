---
title: Join
description: Learn how to use the join method in Kysely.
---

## Basic usage

```ts
await db
  .selectFrom('person')
  .innerJoin('pet', 'pet.owner_id', 'person.id')
  .selectAll('person')
  .select('pet.name')
  .execute()
```
