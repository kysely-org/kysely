{
  "type": "module",
  "scripts": {
    "precheck:types": "cd ../.. && pnpm build && cp -r dist test/composite-ts/node_modules/kysely/",
    "check:types": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "kysely": "^0.28.7"
  }
}
