---
title: 'Insert a single record'
---

# Insert a single record

To insert a record use `.insertInto(table).values(row)`.

import {
  Playground,
  exampleSetup,
  exampleInsertSingleRecord
} from '../../../src/components/Playground'

<Playground code={exampleInsertSingleRecord} setupCode={exampleSetup} />
