---
title: 'Delete a record by ID'
---

# Update a record by ID

Notice that when updating we use `updateTable` instead of `selectFrom`, and the `.set` goes before the `.where`.

import {
  Playground,
  exampleUpdateById,
} from '../../../src/components/Playground'

<Playground ts={exampleUpdateById} />
