---
title: 'Find a record by id'
---

# Find a record by id

To find a record by id use the `.where` method with the `=` operator as follows.

import {
  Playground,
  exampleSetup,
  exampleFindARecordById
} from '../../../src/components/Playground'

<Playground code={exampleFindARecordById} setupCode={exampleSetup} />
