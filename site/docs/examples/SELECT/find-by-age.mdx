---
sidebar_position: 2
title: 'Find all records matching a condition'
---

# Find all records

Notice the use of `.selectAll` which translates to `SELECT * FROM` in SQL.

import {
  Playground,
  exampleFindAllByAge,
} from '../../../src/components/Playground'

<Playground ts={exampleFindAllByAge} />
