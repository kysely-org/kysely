---
title: 'Function calls'
---

# Function calls

This example uses the `fn` module to select some aggregates:

import {
  Playground,
  exampleSetup,
} from '../../../src/components/Playground'

import {
  functionCalls
} from './0060-function-calls'

<Playground code={functionCalls} setupCode={exampleSetup} />
