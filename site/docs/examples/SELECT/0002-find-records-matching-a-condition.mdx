---
title: 'Find records matching a condition'
---

# Find records matching a condition

In this example we're using `.where` to find all persons aged between 18 and 60. 
As you can see, multiple calls to `where` get connected by `AND`. Also notice
the use of `.selectAll` which translates to `SELECT * FROM` in SQL.

import {
  Playground,
  exampleSetup,
  exampleFindRecordsMatchingACondition,
} from '../../../src/components/Playground'

<Playground code={exampleFindRecordsMatchingACondition} setupCode={exampleSetup} />
