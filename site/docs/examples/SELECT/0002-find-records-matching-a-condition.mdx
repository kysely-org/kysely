---
title: 'Find records matching a condition'
---

# Find records matching a condition

In this example we're using `.where` to find all users whose age is > 18.
Notice the use of `.selectAll` which translates to `SELECT * FROM` in SQL.

import {
  Playground,
  exampleSetup,
  exampleFindRecordsMatchingACondition,
} from '../../../src/components/Playground'

<Playground code={exampleFindRecordsMatchingACondition} setupCode={exampleSetup} />
