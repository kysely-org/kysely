---
title: 'Select all columns'
---

# Select all columns

The `selectAll` method produces a `SELECT *` statement.

import {
  Playground,
  exampleSetup,
  exampleSelectAllColumns
} from '../../../src/components/Playground'

<Playground code={exampleSelectAllColumns} setupCode={exampleSetup} />
