---
title: 'Find records by id'
---

# Find records by id

To find multiple records by id we can use the SQL `IN` operator as follows:

import {
  Playground,
  exampleSetup,
  exampleFindRecordsById,
} from '../../../src/components/Playground'

<Playground code={exampleFindRecordsById} setupCode={exampleSetup} />
