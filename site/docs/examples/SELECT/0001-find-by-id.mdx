---
title: 'Find a record by ID'
---

# Find a person by ID

To find a record by ID use the `.where` method with the `=` operator as follows.

import {
  Playground,
  exampleSetup,
  exampleFindById
} from '../../../src/components/Playground'

<Playground code={exampleFindById} setupCode={exampleSetup} />
