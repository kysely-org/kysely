---
title: 'Find all records matching a condition'
---

# Find all records

In this example we're using `.where` to find all users whose age is > 18.
Notice the use of `.selectAll` which translates to `SELECT * FROM` in SQL.

import {
  Playground,
  exampleSetup,
  exampleFindAllByAge,
} from '../../../src/components/Playground'

<Playground code={exampleFindAllByAge} setupCode={exampleSetup} />
