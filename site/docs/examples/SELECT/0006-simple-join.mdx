---
title: 'Simple join'
---

# Simple join

We support several types of joins. In this example we're doing an `innerJoin`.

import {
  Playground,
  exampleSetup,
  exampleSimpleJoin,
} from '../../../src/components/Playground'

<Playground code={exampleSimpleJoin} setupCode={exampleSetup} />