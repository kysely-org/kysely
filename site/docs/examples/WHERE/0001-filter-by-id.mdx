---
title: 'Filter by id'
---

# Filter by id

To find a record by id use the `.where` method with the `=` operator as follows.

import {
  Playground,
  exampleSetup,
  exampleFilterById
} from '../../../src/components/Playground'

<Playground code={exampleFilterById} setupCode={exampleSetup} />
