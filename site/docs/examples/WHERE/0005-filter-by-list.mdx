---
title: 'Filter by list'
---

# Filter by list

In this example we find multiple records using a `WHERE IN` filter.

import {
  Playground,
  exampleSetup,
  exampleFilterByList,
} from '../../../src/components/Playground'

<Playground code={exampleFilterByList} setupCode={exampleSetup} />
