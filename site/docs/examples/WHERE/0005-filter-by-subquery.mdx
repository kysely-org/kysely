---
title: 'Filter by subquery'
---

# Filter by subquery

This example finds all persons who have a dog as a pet using a subquery.

import {
  Playground,
  exampleSetup,
  exampleFilterBySubquery
} from '../../../src/components/Playground'

<Playground code={exampleFilterBySubquery} setupCode={exampleSetup} />
